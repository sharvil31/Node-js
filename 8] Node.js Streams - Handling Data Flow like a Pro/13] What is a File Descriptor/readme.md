### What is a File Descriptor

A File Descriptor(fd) is a non negative integer which represents a opened file. It is like an address of a file and it is assigned by the OS to every file. It starts from 3 because 0, 1, and 2 are occupied by stdin, stdout and stderr. All programming laguages has file descriptor and stdin, stdout, stderr concepts.