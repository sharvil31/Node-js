### How Browsers use Streams

Behind the scenes browsers also uses Streams to downlaod any assets or if we open any web page it has many assets like js file, html & css files, images, these assets are loaded from server to browser using streams. Videos are loaded using streams. Video streaming itself means video is loading through a stream.